# See the root README

The canonical, up-to-date project README lives at
[`/README.md`](../README.md) (the repository root), not here.

This file previously contained an outdated early draft that had fallen out
of sync with the real README and was causing confusion — removed rather
than left to mislead anyone opening `docs/` first.

For setup, architecture, run instructions, testing history, known issues,
and the automated test harness, see the root `README.md`.
